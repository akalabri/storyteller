# Studio API Key
# GOOGLE_API_KEY=AIzaSyARpuAeR974V59tiCkAkTJAibODL6sSKoU

# Vertex AI API Key
GOOGLE_API_KEY=
GOOGLE_APPLICATION_CREDENTIALS="key.json"
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_CLOUD_PROJECT=challengegemini
project_number=1043049322309


# MINIO
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=


# USING VERTEX AI or Studio API
GOOGLE_GENAI_USE_VERTEXAI=true

# VEO Bucket
VEO_BUCKET=challengegemini-storyteller

LTX_API_KEY=
FAL_API_KEY=

ELEVENLABS_API_KEY=

# Dev / pipeline
SEQUENTIAL_GENERATION=0

# DEV_MODE: set to 1 to enable dev mode (backend + frontend; frontend reads this via /api/dev-mode).
# DEV_STEPS: comma-separated list of steps to actually run.
# All other steps are loaded from sessions/DEV_SESSION_ID/ (errors if missing).
# Valid step names: story_breakdown, narration, character_images, scene_prompts,
#                   scene_images, scene_videos, compile
# Example: only run story breakdown + character images:
#   DEV_STEPS=story_breakdown,character_images
DEV_MODE=0
DEV_SESSION_ID=dev_session
DEV_STEPS=story_breakdown